(function(){
const $=id=>document.getElementById(id),file=$('file'),process=$('process'),status=$('status');let text='';
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const norm=v=>String(v??'').toLowerCase().replace(/&/g,' and ').replace(/\b(residence|residential|program|dept|department|house|home)\b/g,' ').replace(/[^a-z0-9]+/g,' ').trim().replace(/\s+/g,' ');
function parseCSV(s){const rows=[];let row=[],cell='',q=false;for(let i=0;i<s.length;i++){const c=s[i];if(q){if(c==='"'&&s[i+1]==='"'){cell+='"';i++}else if(c==='"')q=false;else cell+=c}else if(c==='"')q=true;else if(c===','){row.push(cell);cell=''}else if(c==='\n'){row.push(cell.replace(/\r$/,''));rows.push(row);row=[];cell=''}else cell+=c}if(cell.length||row.length){row.push(cell.replace(/\r$/,''));rows.push(row)}return rows.filter(r=>r.some(v=>String(v).trim()!==''))}
function pick(headers,patterns){const hs=headers.map(norm);for(const p of patterns){const i=hs.findIndex(h=>p.test(h));if(i>=0)return i}return -1}
async function getHomes(){if(!window.__hascClient){if(!window.supabase||!window.HASC_CONFIG)throw new Error('HASC database connection unavailable');window.__hascClient=window.supabase.createClient(window.HASC_CONFIG.SUPABASE_URL,window.HASC_CONFIG.SUPABASE_ANON_KEY)}const q=await window.__hascClient.from('resi_individuals').select('home_name').order('home_name',{ascending:true});if(q.error)throw q.error;return [...new Set((q.data||[]).map(r=>String(r.home_name||'').trim()).filter(Boolean))]}
function resolve(dep,homes){const nd=norm(dep);if(!nd)return null;const exact=homes.filter(h=>norm(h)===nd);if(exact.length===1)return exact[0];const hit=homes.filter(h=>{const nh=norm(h);return nh&&(nd.includes(nh)||nh.includes(nd))});return hit.length===1?hit[0]:null}
function setStatus(m){status.innerHTML=m}
file.addEventListener('change',async()=>{const f=file.files&&file.files[0];process.disabled=true;text='';if(!f)return;try{text=await f.text();const r=parseCSV(text);setStatus('<b>'+esc(f.name)+'</b> loaded — '+Math.max(0,r.length-1)+' rows ready. Click <b>Process Payroll</b>.');process.disabled=false}catch(e){setStatus('Could not read file: '+esc(e.message))}});
process.addEventListener('click',async()=>{process.disabled=true;setStatus('Processing payroll against the HASC residence database…');try{const raw=parseCSV(text);if(raw.length<2)throw new Error('CSV has no data rows');const headers=raw[0].map(v=>String(v).trim());const depIdx=pick(headers,[/^department$/, /department name/, /dept/, /location/, /program/, /residence/, /home/]);const otIdx=pick(headers,[/^ot$/, /overtime hours/, /ot hours/, /overtime/, /ot hrs/, /premium hours/]);if(depIdx<0)throw new Error('Could not identify department column');if(otIdx<0)throw new Error('Could not identify overtime-hours column');const homes=await getHomes();const totals=new Map(),unmatched=new Map();let total=0,used=0;for(let i=1;i<raw.length;i++){const r=raw[i],dep=String(r[depIdx]||'').trim();const hrs=parseFloat(String(r[otIdx]||'').replace(/[$,]/g,''));if(!dep||!Number.isFinite(hrs)||hrs===0)continue;used++;total+=hrs;const home=resolve(dep,homes);if(home){const cur=totals.get(home)||{home,hrs:0,deps:new Set()};cur.hrs+=hrs;cur.deps.add(dep);totals.set(home,cur)}else unmatched.set(dep,(unmatched.get(dep)||0)+hrs)}const rows=[...totals.values()].sort((a,b)=>a.home.localeCompare(b.home));let html=rows.map(x=>'<tr><td>'+esc(x.home)+'</td><td>'+esc([...x.deps].join(', '))+'</td><td class="num">'+x.hrs.toFixed(2)+'</td><td class="good">Matched</td></tr>').join('');html+=[...unmatched.entries()].sort((a,b)=>a[0].localeCompare(b[0])).map(([d,h])=>'<tr><td>—</td><td>'+esc(d)+'</td><td class="num">'+h.toFixed(2)+'</td><td class="bad">Needs attention</td></tr>').join('');$('results').innerHTML=html||'<tr><td colspan="4">No overtime rows found.</td></tr>';$('rows').textContent=used;$('homes').textContent=rows.length;$('hours').textContent=total.toFixed(2);$('miss').textContent=unmatched.size;localStorage.setItem('friday_ot_current_payroll',JSON.stringify({processed_at:new Date().toISOString(),rows:rows.map(x=>({residence:x.home,ot_hours:+x.hrs.toFixed(2),departments:[...x.deps]})),unmatched:[...unmatched].map(([department,ot_hours])=>({department,ot_hours:+ot_hours.toFixed(2)}))}));setStatus('Processed <b>'+used+'</b> OT rows into <b>'+rows.length+'</b> residence totals.'+(unmatched.size?' <b>'+unmatched.size+'</b> department(s) need attention.':' Every overtime department matched.'))}catch(e){setStatus('Processing stopped: '+esc(e.message||String(e)))}process.disabled=false});
})();